import React from 'react';

// have frame class attached to each frame - add css that forms the grid shape.
// each frame has two containing divs that will hold the score from the button press


function Frames(props) {
  return (
    <div className='frame'>
      Frames component
    </div>
  )
}

export default Frames;
